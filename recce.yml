# [Optional] GitHub settings
github:
  repo: "DataRecce/jaffle_shop_duckdb"

# Recce Preset Checks
checks:
  - name: Foo Schema diff of customer_order_pattern
    description: ""
    type: schema_diff
    params:
      node_id: model.jaffle_shop.customer_order_pattern
